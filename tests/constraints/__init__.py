"""Constraint tests. Requires PostgreSQL 16 + RDKit via Docker (testcontainers).

Marked as a package (this file) so the relative ``from .factories import ...``
imports in these test modules resolve under pytest's default import mode.
"""
